import { TitleSlide } from "../../../components/TitleSlide";
import { CONTENT } from "../content";

export const OutroSegment: React.FC = () => {
  return <TitleSlide title={CONTENT.outroText} />;
};
